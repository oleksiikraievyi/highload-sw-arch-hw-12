# highload-sw-arch-hw-12

# Prerequisites
* docker
* linux + bash

# 1. Run Redis instances with rdb and aof + beanstalkd and run benchmark
```
./run.sh
```

# 2. Clean up
```
./cleanup.sh
```